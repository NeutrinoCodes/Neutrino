__kernel void thekernel(__global  float4*       points,
                        __global  float4*       colors,
                                  unsigned int  num_points)
{
  int i = get_global_id(0);

  points[i].x = 2.0f*(float)i/(float)num_points - 1.0f;
  points[i].y = 0.0f;
  points[i].z = 0.0f;
  points[i].w = 1.0f;

  colors[i] = 1.0f;
}
