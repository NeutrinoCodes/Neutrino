__kernel void thekernel(__global  float4*       points)
{
  int i = get_global_id(0);

  points[i].x = 2.0f*(float)i/10.0 - 1.0f;                                      // 10.0 = ten points.
  points[i].y = 0.0f;
  points[i].z = 0.0f;
  points[i].w = 1.0f;
}
