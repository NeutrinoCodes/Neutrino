__kernel void thekernel(__global  float4*       points,
                        __global  float4*       colors,
                        unsigned  int           points_size)
{
  int i = get_global_id(0);

  //points[i].x = 2.0f*(float)i/points_size - 1.0f;
  //points[i].y = 0.0f;
  //points[i].z = 0.0f;
  //points[i].w = 1.0f;
}
